OpenClaw DocOps Skill

This folder provides an OpenClaw skill wrapper around the repo CLI commands.

Install
Copy this folder into your OpenClaw skills directory and reload OpenClaw.

Requirements
node and npm are available.
Repo dependencies are installed.
A .env is configured for Qdrant Cloud and OpenAI.
