Cursor Prompt

We hit npm EJSONPARSE on package.json: Unexpected non-whitespace character after JSON. Please inspect package.json for trailing non-whitespace after the final closing brace, likely a literal backslash-n sequence. Provide a minimal diff that rewrites package.json as valid JSON, preserving all fields and scripts, ending with a real newline.
